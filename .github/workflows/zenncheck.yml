name: Check Zenn API and Deploy

on:
  schedule:
    - cron: "0 */3 * * *"
  workflow_dispatch:

permissions:
  contents: read
  actions: write

jobs:
  check-zenn-updates:
    runs-on: ubuntu-latest

    outputs:
      has-changes: ${{ steps.check-diff.outputs.has-changes }}

    steps:
      # 前回のハッシュ値を復元
      - name: Restore previous Zenn hash
        id: cache-zenn
        uses: actions/cache/restore@v4
        with:
          path: zenn-hash.txt  # ← ファイル名を統一
          key: zenn-hash-${{ github.run_id }}  # ← 一意なkeyにしない（常に同じkeyを使う）
          restore-keys: |
            zenn-hash-  # ← プレフィックスで最新のキャッシュを復元

      # Zenn APIから最新記事を取得してハッシュ化
      - name: Fetch and hash Zenn data
        run: |
          echo "📡 Zenn APIから最新記事を取得中..."

          if ! curl -sf --max-time 30 "https://zenn.dev/api/articles?username=gouki_main&order=latest&page=1" \
               | sha256sum | cut -d' ' -f1 > zenn-hash-new.txt; then
            echo "❌ Zenn APIへのリクエストが失敗しました"
            exit 1
          fi

          echo "✅ ハッシュ値: $(cat zenn-hash-new.txt)"

      # 前回のハッシュ値と比較
      - name: Check for changes
        id: check-diff
        run: |
          if [ ! -f zenn-hash.txt ]; then
            echo "⚠️ 前回のハッシュが見つかりません(初回実行またはキャッシュ期限切れ)"
            echo "has-changes=true" >> $GITHUB_OUTPUT
          elif diff -q zenn-hash.txt zenn-hash-new.txt > /dev/null; then
            echo "✅ 変更なし: Zenn記事に更新はありません"
            echo "has-changes=false" >> $GITHUB_OUTPUT
          else
            echo "🔄 変更あり: Zenn記事が更新されました"
            echo "前回: $(cat zenn-hash.txt)"
            echo "今回: $(cat zenn-hash-new.txt)"
            echo "has-changes=true" >> $GITHUB_OUTPUT
          fi

      # 新しいハッシュ値を次回用のファイル名にコピー
      - name: Prepare hash for caching
        if: success()
        run: cp zenn-hash-new.txt zenn-hash.txt

      # 現在のハッシュ値を次回用に保存
      - name: Save current Zenn hash
        if: success()
        uses: actions/cache/save@v4
        with:
          path: zenn-hash.txt  # ← ファイル名を統一
          key: zenn-hash-${{ github.run_id }}  # ← 実行ごとに一意なkeyにする

      # 差分がある場合はデプロイワークフローをトリガー
      - name: Trigger deployment workflow
        if: steps.check-diff.outputs.has-changes == 'true'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          echo "📝 Zenn記事に更新がありました。デプロイを開始します。"
          gh workflow run deploy.yml \
            --repo ${{ github.repository }} \
            --ref ${{ github.ref_name }}